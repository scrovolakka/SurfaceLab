# --- macOS / editor junk ---
.DS_Store
*.swp
*~

# --- Build artifacts (regenerated by CMake/Ninja/Rez) ---
build/
*.plugin
*.rsrc
*.o
*.obj

# --- Working tree ---
# Contains the licensed Adobe After Effects SDK (work/vendor — MUST NOT be
# committed/redistributed), 100M+ of build output, and ephemeral
# staging/verify/packaging/backup directories. All regenerable or licensed.
/work/

# --- Release archives ---
# Source + binary zips reproduced from tagged source. Kept out of git history;
# use GitHub Releases for published builds instead.
/outputs/
